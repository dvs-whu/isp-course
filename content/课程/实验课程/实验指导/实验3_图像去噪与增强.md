# 实验3：图像去噪与增强

> **课程名称**：智能信号处理  
> **实验学时**：2学时（W6）  
> **实验类型**：综合性实验

---

## 一、实验目标

1. 掌握二维离散傅里叶变换（2D-FFT）的原理及其在图像处理中的应用
2. 理解频域滤波的基本原理，能够设计并实现频率域低通滤波器
3. 学会使用空间域滤波方法（均值、中值、高斯）对图像进行去噪处理
4. 掌握PSNR和SSIM两种图像质量评价指标的计算方法

---

## 二、实验环境

| 软件/库 | 版本要求 |
|---------|---------|
| Python | ≥ 3.8 |
| NumPy | ≥ 1.21 |
| SciPy | ≥ 1.7 |
| Matplotlib | ≥ 3.5 |
| scikit-image | ≥ 0.19 |

```bash
pip install numpy scipy matplotlib scikit-image
```

---

## 三、实验步骤

### 任务1：图像加载与二维频谱可视化

读取灰度图像，执行二维FFT变换，并可视化其频谱。

```python
import numpy as np
import matplotlib.pyplot as plt
from skimage import io, color

# 读取图像并转为灰度
img = io.imread('test_image.png')
if img.ndim == 3:
    img = color.rgb2gray(img)

# TODO: 执行二维傅里叶变换
F = np.fft.fft2(____)          # 补全参数
F_shift = np.fft.fftshift(____)  # 将零频移到中心

# TODO: 计算幅度谱（取对数增强显示效果）
magnitude_spectrum = np.log(____ + 1)  # 补全公式

# 可视化
fig, axes = plt.subplots(1, 2, figsize=(10, 5))
axes[0].imshow(img, cmap='gray')
axes[0].set_title('原始图像')
axes[1].imshow(magnitude_spectrum, cmap='gray')
axes[1].set_title('频谱')
plt.savefig('spectrum.png', dpi=150)
plt.show()
```

### 任务2：频域高斯低通滤波去噪

设计高斯低通滤波器，在频域中对带噪图像进行去噪。

```python
from skimage import data, util

# 添加高斯噪声
img_clean = img.copy()
img_noisy = util.random_noise(img_clean, mode='gaussian', var=0.01)

# 对带噪图像做FFT
F_noisy = np.fft.fft2(img_noisy)
F_noisy_shift = np.fft.fftshift(F_noisy)

# TODO: 设计高斯低通滤波器
M, N = img.shape
D0 = 30  # 截止频率，可尝试不同值
u = np.arange(M) - M // 2
v = np.arange(N) - N // 2
U, V = np.meshgrid(v, u)
D = np.sqrt(____)  # 计算到中心的距离 D(u,v)
H = np.exp(____)   # 高斯低通滤波器传递函数

# TODO: 频域滤波
G = F_noisy_shift * ____
g = np.abs(np.fft.ifft2(np.fft.ifftshift(____)))  # 逆变换回空间域

# 显示结果
fig, axes = plt.subplots(1, 3, figsize=(15, 5))
axes[0].imshow(img_clean, cmap='gray'); axes[0].set_title('原始图像')
axes[1].imshow(img_noisy, cmap='gray'); axes[1].set_title('带噪图像')
axes[2].imshow(g, cmap='gray'); axes[2].set_title(f'频域去噪 (D0={D0})')
plt.savefig('freq_denoise.png', dpi=150)
plt.show()
```

### 任务3：空间域滤波方法对比

分别使用均值滤波、中值滤波和高斯滤波对带噪图像进行去噪，并比较效果。

```python
from scipy.ndimage import uniform_filter, median_filter, gaussian_filter

# TODO: 使用三种空间域滤波器进行去噪
img_mean = uniform_filter(img_noisy, size=____)       # 均值滤波，窗口大小5
img_median = median_filter(img_noisy, size=____)      # 中值滤波，窗口大小5
img_gaussian = gaussian_filter(img_noisy, sigma=____)  # 高斯滤波，sigma=1

# 可视化对比
fig, axes = plt.subplots(2, 3, figsize=(15, 10))
titles = ['原始图像', '带噪图像', '频域去噪',
          '均值滤波', '中值滤波', '高斯滤波']
images = [img_clean, img_noisy, g, img_mean, img_median, img_gaussian]
for ax, title, im in zip(axes.flat, titles, images):
    ax.imshow(im, cmap='gray')
    ax.set_title(title)
    ax.axis('off')
plt.tight_layout()
plt.savefig('spatial_compare.png', dpi=150)
plt.show()
```

### 任务4：PSNR与SSIM评价

定量评价各去噪方法的效果。

```python
from skimage.metrics import peak_signal_noise_ratio as psnr
from skimage.metrics import structural_similarity as ssim

# TODO: 计算各方法的PSNR和SSIM（与原始图像对比）
results = []
methods = ['带噪图像', '频域去噪', '均值滤波', '中值滤波', '高斯滤波']
denoised = [img_noisy, g, img_mean, img_median, img_gaussian]

for name, im in zip(methods, denoised):
    p = psnr(img_clean.astype(np.float64), im.astype(np.float64))
    s = ssim(____, ____, data_range=1.0)  # 补全参数
    results.append((name, p, s))

# 填写评价结果表
print(f"{'方法':<12} {'PSNR(dB)':<12} {'SSIM':<12}")
print('-' * 36)
for name, p, s in results:
    print(f"{name:<12} {p:<12.2f} {s:<12.4f}")
```

**评价结果表**（请将实验数据填入下表）：

| 方法 | PSNR (dB) | SSIM |
|------|-----------|------|
| 带噪图像 | | |
| 频域去噪 | | |
| 均值滤波 | | |
| 中值滤波 | | |
| 高斯滤波 | | |

---

## 四、思考题

1. 高斯低通滤波器的截止频率 $D_0$ 对去噪效果和图像清晰度有何影响？试从频域角度解释。
2. 在去除高斯噪声和椒盐噪声时，均值滤波与中值滤波各自的优势是什么？为什么？
3. PSNR和SSIM两种评价指标有何异同？在什么场景下SSIM更能反映人眼的主观感受？

---

## 五、实验报告要求

1. 提交完整的Python源代码（含注释）
2. 包含频谱图、去噪结果对比图、评价指标表
3. 对比分析频域滤波与空间域滤波的去噪效果，结合PSNR/SSIM数据进行讨论
4. 回答思考题，每题不少于100字
5. 报告格式：PDF，命名规则：`学号_姓名_实验3.pdf`

---

> **课程关联**：本实验安排在[[欢迎|智能信号处理课程]]W6。涉及知识点：[[知识点/经典信号处理/傅里叶分析|傅里叶分析]]（二维FFT）、[[知识点/经典信号处理/数字滤波器设计|数字滤波器设计]]（频域滤波器）、[[知识点/图像信号处理/图像去噪|图像去噪]]、[[知识点/图像信号处理/图像重建|图像重建]]。关联项目：[[课程/项目库/项目一/项目说明书_迷你项目C_图像频域|项目一C]]。
