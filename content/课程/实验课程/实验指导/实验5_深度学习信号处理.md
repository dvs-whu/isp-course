# 实验5：深度学习信号处理

> **课时**：2学时 | **周次**：W9

---

## 一、实验目标

1. 掌握 PyTorch 张量（Tensor）的基本操作，包括创建、索引、变形和自动求导。
2. 理解卷积神经网络（CNN）的基本结构，能够使用 `torch.nn` 搭建简单的 CNN 模型。
3. 熟悉完整的模型训练流程：数据加载、前向传播、损失计算、反向传播与参数更新。
4. 学会使用 `matplotlib` 绘制训练过程中的损失曲线和准确率曲线，分析模型收敛情况。

---

## 二、实验环境

- Python 3.8+
- PyTorch >= 1.12
- torchvision
- matplotlib
- numpy

安装命令：
```bash
pip install torch torchvision matplotlib numpy
```

---

## 三、实验步骤

### 任务一：PyTorch 张量基础操作

```python
import torch

# 1. 创建张量
a = torch.randn(3, 4)          # 随机张量
b = torch.zeros(3, 4)          # 全零张量
c = torch.ones(3, 4)           # 全一张量
d = torch.arange(0, 12).reshape(3, 4)  # 序列张量

print("a:\n", a)
print("d:\n", d)

# 2. 基本运算
e = a + c                      # 逐元素加法
f = torch.matmul(a, d.T)       # 矩阵乘法 (3x4) @ (4x3) = 3x3
print("f:\n", f)

# 3. 索引与切片
print("第2行:", a[1, :])
print("子矩阵:\n", a[0:2, 1:3])

# 4. 自动求导
x = torch.tensor([2.0, 3.0], requires_grad=True)
y = x[0]**2 + x[1]**3          # y = x0^2 + x1^3
y.backward()                   # 反向传播
print("dy/dx:", x.grad)        # 应为 [4.0, 27.0]

# TODO: 尝试用 GPU 加速张量运算
# device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
# a_gpu = a.to(device)
# print(a_gpu.device)
```

**练习**：计算 `y = sin(x) + x^2` 在 `x=1.0` 处的导数，验证结果。

---

### 任务二：搭建 CNN 识别 MNIST 手写数字

```python
import torch
import torch.nn as nn
import torch.optim as optim
from torchvision import datasets, transforms
from torch.utils.data import DataLoader

# 数据预处理与加载
transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.1307,), (0.3081,))
])

train_dataset = datasets.MNIST('./data', train=True, download=True, transform=transform)
test_dataset  = datasets.MNIST('./data', train=False, transform=transform)
train_loader  = DataLoader(train_dataset, batch_size=64, shuffle=True)
test_loader   = DataLoader(test_dataset, batch_size=1000)

# 定义 CNN 模型：2个卷积层 + 2个全连接层
class SimpleCNN(nn.Module):
    def __init__(self):
        super(SimpleCNN, self).__init__()
        self.conv1 = nn.Conv2d(1, 16, kernel_size=3, padding=1)   # 输入1通道，输出16通道
        self.conv2 = nn.Conv2d(16, 32, kernel_size=3, padding=1)  # 输入16通道，输出32通道
        self.pool  = nn.MaxPool2d(2, 2)                           # 2x2 最大池化
        self.fc1   = nn.Linear(32 * 7 * 7, 128)                   # 全连接层1
        self.fc2   = nn.Linear(128, 10)                            # 输出10类
        self.relu  = nn.ReLU()

    def forward(self, x):
        x = self.pool(self.relu(self.conv1(x)))   # 28x28 -> 14x14
        x = self.pool(self.relu(self.conv2(x)))   # 14x14 -> 7x7
        x = x.view(-1, 32 * 7 * 7)               # 展平
        x = self.relu(self.fc1(x))
        x = self.fc2(x)
        return x

model = SimpleCNN()
print(model)

# TODO: 统计模型参数量
# total_params = sum(p.numel() for p in model.parameters())
# print(f"总参数量: {total_params}")
```

---

### 任务三：训练循环与可视化

```python
import matplotlib.pyplot as plt

criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)

# 训练与测试
train_losses, test_accs = [], []
num_epochs = 5

for epoch in range(num_epochs):
    # --- 训练阶段 ---
    model.train()
    running_loss = 0.0
    for images, labels in train_loader:
        optimizer.zero_grad()               # 梯度清零
        outputs = model(images)             # 前向传播
        loss = criterion(outputs, labels)   # 计算损失
        loss.backward()                     # 反向传播
        optimizer.step()                    # 更新参数
        running_loss += loss.item()

    avg_loss = running_loss / len(train_loader)
    train_losses.append(avg_loss)

    # --- 测试阶段 ---
    model.eval()
    correct, total = 0, 0
    with torch.no_grad():
        for images, labels in test_loader:
            outputs = model(images)
            _, predicted = torch.max(outputs, 1)
            total += labels.size(0)
            correct += (predicted == labels).sum().item()

    acc = 100.0 * correct / total
    test_accs.append(acc)
    print(f"Epoch [{epoch+1}/{num_epochs}]  Loss: {avg_loss:.4f}  测试准确率: {acc:.2f}%")

# 可视化
fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 4))
ax1.plot(train_losses, 'b-o'); ax1.set_title('训练损失'); ax1.set_xlabel('Epoch')
ax2.plot(test_accs, 'r-o');    ax2.set_title('测试准确率'); ax2.set_xlabel('Epoch')
plt.tight_layout(); plt.savefig('training_curves.png'); plt.show()

# TODO: 尝试调整学习率或网络结构，观察曲线变化
```

---

## 四、思考题

1. 本实验中卷积层的输出尺寸是如何计算的？请写出 `conv1` 和 `conv2` 后的特征图大小。
2. 如果将 `MaxPool2d(2,2)` 替换为 `AvgPool2d(2,2)`，对模型性能可能有什么影响？为什么？
3. 对比全连接网络和卷积网络处理图像的优劣，从参数量和特征提取能力两方面分析。

---

## 五、实验报告要求

1. 提交 Jupyter Notebook 或 Python 脚本，代码需附中文注释。
2. 截图展示张量操作结果、模型结构摘要、训练损失与准确率曲线。
3. 在报告中回答思考题，每题不少于 100 字。
4. 记录实验中遇到的问题及解决方法（如有）。

---

> **课程关联**：本实验安排在[[欢迎|智能信号处理课程]]W9。涉及知识点：[[知识点/深度学习/深度学习基础|深度学习基础]]。关联项目：[[课程/项目库/项目二/项目二_跨学科项目选题指南|项目二]]。
